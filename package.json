{
  "name": "flora",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3000",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "axios": "^1.12.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "json-server": "^0.17.4",
    "vite": "^7.1.3"
  }
}
